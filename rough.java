package Bank;

import java.sql.ResultSet;
import java.util.Scanner;

public class rough {
    String query ="insert into bank(account_number,account_holder_name,bank_name ,bank_location,mobile_number ,holder_adderess,account_type,amount)values (?,?,?,?,?,?,?,?)";
    //Scanner sc=new Scanner(System .in);
       // connection.prepareStatement(query);
    //ResultSet resultSet  = preparedStatement.executeQuery();
}
